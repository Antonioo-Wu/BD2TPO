<!DOCTYPE html>
<html>
<head>
    <title>Biblioteca - Inicio</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            text-align: center;
        }
        h1 {
            color: #333;
            margin-bottom: 30px;
        }
        .welcome-message {
            font-size: 18px;
            color: #666;
            margin-bottom: 30px;
        }
        .actions {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
        }
        .btn {
            padding: 15px 25px;
            border-radius: 4px;
            text-decoration: none;
            color: white;
            font-weight: bold;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: transform 0.2s, background-color 0.3s;
        }
        .btn:hover {
            transform: translateY(-2px);
        }
        .btn-catalogo {
            background-color: #007bff;
        }
        .btn-catalogo:hover {
            background-color: #0056b3;
        }
        .btn-prestamos {
            background-color: #6f42c1;
        }
        .btn-prestamos:hover {
            background-color: #5a32a3;
        }
        .btn-carrito {
            background-color: #28a745;
        }
        .btn-carrito:hover {
            background-color: #218838;
        }
        .user-info {
            margin-top: 20px;
            padding: 10px;
            background-color: #f8f9fa;
            border-radius: 4px;
            color: #666;
        }
        .logout {
            margin-top: 20px;
        }
        .logout-btn {
            color: #dc3545;
            text-decoration: none;
        }
        .logout-btn:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Bienvenido a la Biblioteca</h1>
        
        <div class="welcome-message">
            ¡Hola, <%= usuario.username %>!
        </div>

        <div class="actions">
            <a href="/libros" class="btn btn-catalogo">
                📚 Ver Catálogo
            </a>
            <a href="/prestamos/activos" class="btn btn-prestamos">
                📖 Mis Préstamos
            </a>
            <a href="/carrito" class="btn btn-carrito">
                🛒 Mi Carrito
            </a>
        </div>

        <div class="user-info">
            Conectado como: <%= usuario.username %> (<%= usuario.rol %>)
        </div>

        <div class="logout">
            <a href="/auth/logout" class="logout-btn">Cerrar Sesión</a>
        </div>
    </div>
</body>
</html> 